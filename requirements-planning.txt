# Planning-venv dependencies (CPU side; Kit/Isaac deps come from the Isaac Lab install).
# Versions are the measured working set recorded in docs/environment.md ("Planning stack").
# Install into a --system-site-packages venv created from Kit's python (see docs/environment.md):
#   /workspace/isaaclab/env_isaaclab/bin/pip install -r requirements-planning.txt
ompl==2.0.1
python-fcl==0.7.0.11
coacd==1.0.11
trimesh==4.12.2
matplotlib==3.11.1
imageio==2.37.4

# Preinstalled in the Isaac Sim python (versions measured, not pip-installed here):
#   imageio-ffmpeg 0.6.0 (bundled static ffmpeg 7.0.2), pin (Pinocchio) 4.1.0,
#   pytest 9.1.1 (system site)
# Asset-archive tooling (scripts/tools/, optional; versions not pinned — not part of the
# measured planning stack): huggingface_hub requests pyyaml filelock tqdm fsspec
